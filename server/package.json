{
  "name": "server",
  "version": "1.0.0",
  "description": "The playground server",
  "main": "index.js",
  "scripts": {    
    "build": "tsc --project tsconfig.json",
    "build:watch":"npm run build -- --watch",
    "start":"node dist/index.js",
    "start:watch":"nodemon dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "hapi": "^18.1.0",
    "socket.io": "^2.3.0"
  },
  "devDependencies": {
    "@types/hapi": "^18.0.3",
    "@types/node": "^14.11.2",
    "@types/socket.io": "^2.1.11",
    "nodemon": "^2.0.4",
    "tslint": "^6.1.3",
    "typescript": "^4.0.3"
  }
}
